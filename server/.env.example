PORT=3001

# Required
JWT_SECRET=change-me-to-a-long-random-string
STORAGE_SECRET=change-me-to-a-long-random-string

# Optional: create an admin user on first boot (will NOT reset existing passwords)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me

# Optional: CORS allowlist (comma-separated). In production, leaving empty blocks browser origins.
# Example: CORS_ORIGINS=http://localhost:5173,https://your-domain.com
CORS_ORIGINS=

# Optional: allow private/internal storage endpoints for WebDAV/S3 (development default allows).
# Set true only if you intentionally use LAN endpoints.
ALLOW_PRIVATE_STORAGE_ENDPOINTS=false

# Optional: enable mobile sync endpoints (/api/mobile/* and /api/auth/mobile-key*)
ENABLE_MOBILE=false

